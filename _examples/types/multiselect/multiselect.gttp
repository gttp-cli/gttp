$Options: multiselect {
    Option 1
    Option 2
    Option 3
}

---

Show as array: {{ .Options }}

Loop through array:
{{ range $index, $element := .Options}}
    - index {{ $index }}: {{ $element -}}
{{ end }}
